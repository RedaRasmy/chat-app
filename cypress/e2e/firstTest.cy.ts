describe("template spec", () => {

    beforeEach(()=>{
        
    })

    it("passes", () => {
        cy.visit("/");
        // cy.get('[data-testid="page"]')
        //   .should('exist')
        //   .should('')
    });
});
